{
  "name": "@cybersim/fs",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "wasm:build": "cd wasm && cargo build --target wasm32-unknown-unknown --release"
  },
  "dependencies": {
    "@cybersim/shared": "*",
    "idb": "^8",
    "dexie": "^4"
  },
  "devDependencies": {
    "vitest": "^2",
    "@types/node": "^20"
  }
}