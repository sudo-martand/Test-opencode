{
  "name": "@cybersim/wasm-crypto",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "wasm:build": "cd wasm && cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-dir ../pkg ../target/wasm32-unknown-unknown/release/wasm_crypto.wasm && wasm-opt -Oz ../pkg/wasm_crypto_bg.wasm -o ../pkg/wasm_crypto_bg.wasm",
    "test": "vitest run"
  },
  "dependencies": {
    "@cybersim/shared": "*"
  },
  "devDependencies": {
    "vitest": "^2",
    "@types/node": "^20",
    "wasm-pack": "^0.12"
  }
}